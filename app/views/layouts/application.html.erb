<!DOCTYPE html>
<html>
<head>
  <title>Topbranch</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
</head>
<body class="<%= current_subdomain %>">

<%= render "#{current_subdomain}/navigation" rescue nil %>

<%= yield %>

<%= render "#{current_subdomain}/footer" rescue nil %>

</body>
</html>
